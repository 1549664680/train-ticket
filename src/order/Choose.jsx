import React, { memo } from "react";
import "./Account.css";
import propTypes, { func } from "prop-types";

const Choose = memo(function Choose(props) {
  return <div className="choose"></div>;
});
Choose.propTypes = {};
export default Choose;
